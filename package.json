{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run --reporter=verbose"
  },
  "devDependencies": {
    "sass": "^1.96.0",
    "vitest": "latest"
  }
}
